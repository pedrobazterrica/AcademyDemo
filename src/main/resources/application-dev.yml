spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/viveredemo
    username: postgres
    password: AcademiaVivere
  jpa:
    hibernate:
      ddl-auto: create-drop
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: true
    database: POSTGRESQL